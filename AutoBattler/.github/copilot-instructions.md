# Copilot Instructions

## Project Guidelines
- For benchmark batches, `AgentCounts` represent total agents across both teams, so each team spawner should use half the configured total (e.g., total 10 => 5 per team).